{{#if resume.education.length}}
<h2>Education</h2>
	{{#each resume.education}}		
<h4>{{#if studyType}}{{studyType}}{{/if}} {{#if area}}({{area}}){{/if}}
<p>{{#if institution}}{{institution}}{{/if}} ({{#if startDate}}{{dateFormat startDate format="YYYY"}}{{/if}}{{#if endDate}} - {{dateFormat endDate format="YYYY"}}{{/if}})
{{/each}}
<hr>
{{/if}}